for(let contador = 0; contador < 10; contador++) {
    console.log('O valor do contador é: '+ contador)
}

const notas = [6.6, 10, 4.5, 7.8] // Array

for(let i = 0; i < notas.length; i++){ // I < notas.legth (é o tamanho do array) utilizada para navegar no array
    console.log(`O valor da chave ${i} é: ${notas[i]}`)
}
