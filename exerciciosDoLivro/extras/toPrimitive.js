const pessoa = {
    idade:24
}

console.log( '24' == pessoa.idade ? true: false) /* Com o operador == que compara só 
os valores */

console.log( '24' === pessoa.idade ? true: false) /* Com o operador === que compara os 
valores e o tipo do dado */

console.log('packt' == true)

