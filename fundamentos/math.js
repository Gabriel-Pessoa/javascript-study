const raio = 5.6;

/* Math.PI é uma ferramenta que já tem gravado o valor do PI. Math.pow 
é um operador de potência  */

const area = Math.PI * Math.pow(raio,2); 

console.log(area);
console.log(area.toFixed(2));
console.log(typeof Math);