// Função arrow e operador ternário
const resultado = nota => nota >= 7 ? console.log('Aprovado') : console.log('Reprovado')

resultado(7)
resultado(6.9)

// Ou assim tbm

const teste = nota >= 7 ? 'Aprovado' : 'Reprovado' // Grava o resultado na constante teste

