const [a, b, c, d] = [3, 5, 1 , 15]

// Operadores binários, soma a + b= resultado + c= resultado + d
const soma = a + b + c + d // infix
// ++a //prefix
// a++ //postfix
// +a unário
// -a unário

const subtracao = d-b
const divisao = d/a
const modulo = a % 2 // Dica: se você quiser saber se um resultado ou 
// número é par ou impar use (% 2) resultado 0 é par, resultado 1 é impar

console.log(soma, subtracao, divisao, modulo)
